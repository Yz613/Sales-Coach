# Apex aliases. Next middleware is scoped to /app, so these must also be
# handled by Cloudflare static-asset redirects and the worker wrapper.
/calls /app/calls 308
/calls/* /app/calls/:splat 308
/favicon.ico /app/icon.svg 308
/icon.svg /app/icon.svg 308
